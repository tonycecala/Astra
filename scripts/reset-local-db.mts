console.log("Local reset is intentionally explicit: run against a disposable local Postgres database, then npm run db:migrate && npm run db:seed.");
console.log("No production or preview reset behavior is implemented in the foundation.");
